# 探索记录 (Findings)

## 缓存架构共识 (2026-03-07)

- 放弃外部依赖 (Redis)，坚决贯彻 KISS 原则，首选使用 `.npy` 和 `.pkl` 将模型推理产物本地化。
- 这不仅满足了加速需求，也有助于向学习者展现矩阵持久化和分离存储的结构（白盒化教学目标）。
- 采用包含 `{model_type}_{dataset_name}_embed{embed_dim}` 的全条件缓存命名规范，完美规避了多空间映射下的索引窜乱风险。
- 前端保留“开始双轨平行检索”和全量索引按钮（UI方案A），仅在内部分支判断是否命中缓存，保留了用户的手动控制感。
